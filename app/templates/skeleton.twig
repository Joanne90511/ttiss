<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <meta name="description" content="">
    <meta name="author" content="">

    <title>{% block title %} TTISS {% endblock %}</title>

    <!-- Bootstrap core CSS -->
    <link href="{{ base_url() }}/css/bootstrap.min.css" rel="stylesheet">

    <!--  Font Awesome Glyphs  -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">
    <link href='https://fonts.googleapis.com/css?family=Raleway|Open+Sans|Lato|Roboto' rel='stylesheet' type='text/css'>
    <link rel="stylesheet" href="{{ base_url() }}/css/jquery-ui.min.css">
    <link rel="stylesheet" href="{{ base_url() }}/css/jquery-ui.structure.min.css">


    <link rel="stylesheet" href="{{ base_url() }}/css/jquery-ui.theme.min.css">

    <!-- Custom styles for this template -->
    <link href="{{ base_url() }}/css/simple-sidebar.css" rel="stylesheet">
    {% block css %}  {% endblock %}
    <link href="{{ base_url() }}/css/style.css" rel="stylesheet">


</head>

<body>
{% block belowbody %} {% endblock %}
<nav class="navbar navbar-default">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ base_url() }}">TTISS</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                {% if auth == true %}
                <li><a href="{{ base_url() }}/new">New Entry</a></li>
                <li><a href="{{ base_url() }}/">Previous Entries</a></li>
                {% endif %}
                <li><a target="_blank" href="http://mctr.mcmaster.ca">REDCap</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <ul class="nav navbar-nav navbar-right">
                    {% if auth == true %}
                    <li role="separator" class="divider"></li>
                    <li><a href="{{ base_url() }}/logout">Logout</a></li>
                    {% else %}
                    <li><a href="{{ base_url() }}/login">Login</a></li>
                    {% endif %}
                </ul>
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>
<!-- Page Content -->
<div id="page-content-wrapper">
    <div class="container-fluid">
        <div data-bind='redcapMessenger: saveStatus' style="display: none;" id="messenger">
            <div data-bind='html: saveStatusMessage' class="alert alert-success" role="alert">Successful Saved</div>
        </div>
        {% block content %} {% endblock %}
    </div>{# Container #}
</div>{# wrapper #}

{# TODO check if use cdnjs is okay #}
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.4/jquery.js"></script>

<script src="{{ base_url() }}/js/bootstrap.min.js"></script>
{% block js %}  {% endblock %}
<script src="{{ base_url() }}/js/site.js"></script>
</body >
</html >
